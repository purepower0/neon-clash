# NEON CLASH

A browser-based 2D stick fighting game built with React, TypeScript, Canvas and Vite.

## Run it locally

Requires Node.js 20+ (22 recommended).

```bash
npm install
npm run dev
```

Open the localhost address Vite prints in the terminal.

For a production build:

```bash
npm run build
```